# API Contracts

API contracts will be added incrementally.

The first endpoints will be:

- `GET /health`
- `GET /api/v1/system/status`
- Broker connection, account, instrument, quote, candle, position, and order read endpoints
- Backend risk calculation endpoint
